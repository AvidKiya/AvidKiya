# AvidKiya v1.2.0

تاریخ انتشار: 2026-07-30

## اصلاحات طراحی و UX

- بازطراحی دوباره تقویم برای خوانایی بهتر.
- نمایش تقویم شاهنشاهی با عدد روز خوانا و نام روز اوستایی زیر آن.
- اضافه شدن کارت توضیح «امروز در گاهشمار اوستایی» داخل تقویم.
- اصلاح رنگ متن روی پس‌زمینه سبز/اصلی برای جلوگیری از ناخوانایی.
- اضافه شدن استایل عمومی برای select، انتخاب بودجه، file picker و گزینه‌های انتخابی.
- حذف selectهای ساده و زشت در فرم تماس و خدمات و جایگزینی با کارت‌های انتخاب بودجه.
- اصلاح دکمه انتخاب فایل در فشرده‌سازی عکس و هماهنگ‌کردن آن با طراحی سایت.
- حذف select ساده مرتب‌سازی فروشگاه و جایگزینی با کنترل Pill/Tab.

## مبالغ و واحد پول

- همه قیمت‌های خدمات، تعرفه‌ها و فروشگاه به تومان تبدیل شد.
- محصولات فروشگاه با currency برابر IRR و قیمت تومانی تنظیم شدند.
- تعرفه خدمات و قیمت‌های پنل مدیریت هم تومانی شدند.
- نسخه CMS به v3 و storage key جدید تغییر کرد تا دیتای قدیمی دلاری در مرورگر override نکند.

## پنل مدیریت جدید و امنیت

- پنل مدیریت قدیمی client-side حذف و یک Admin Console جدید ساخته شد.
- ورود با رمز پیش‌فرض admin حذف شد.
- مسیر /kiya/panel با Middleware محافظت می‌شود.
- ورود از مسیر /kiya/login انجام می‌شود.
- احراز هویت با API، Cookie از نوع HttpOnly، SameSite Strict و امضای HMAC انجام می‌شود.
- خروج امن از پنل اضافه شد.
- فایل .env.example برای ADMIN_PASSWORD و ADMIN_SECRET به‌روزرسانی شد.

## تست

Build پروژه با موفقیت انجام شد:

```bash
npm run build
```

## نکته مهم دیپلوی

برای فعال شدن پنل روی Cloudflare، این Environment Variableها را حتماً تنظیم کنید:

```text
ADMIN_PASSWORD=your-very-strong-private-password
ADMIN_SECRET=your-long-random-secret
```

بدون این مقادیر، ورود پنل عمداً غیرفعال می‌ماند.
