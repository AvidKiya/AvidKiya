# AvidKiya v1.6.1

تاریخ انتشار: 2026-07-30

## نشانگر موس

- سایز پیش‌فرض Ghost Cursor از حالت بزرگ به `0.03` کاهش پیدا کرد.
- تنظیمات نشانگر موس به CMS اضافه شد.
- نسخه CMS به v5 و storage key به `avidkiya_cms_v5` تغییر کرد تا تنظیمات جدید بدون تداخل با داده قدیمی لود شود.

## پنل مدیریت

- بخش جدید «نشانگر موس» به پنل ادمین اضافه شد.
- از پنل می‌توان موارد زیر را تغییر داد:
  - فعال/غیرفعال بودن نشانگر
  - سایز نشانگر
  - تعداد نقاط دنباله
  - Spring
  - Friction
  - رنگ اصلی
  - رنگ حاشیه
  - حالت Flat Color
- تغییرات بعد از ذخیره CMS روی سایت اعمال می‌شوند.

## تست

Build پروژه با موفقیت انجام شد:

```bash
npm run build
```
