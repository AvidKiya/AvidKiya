name = "avidkiya-portfolio"
compatibility_date = "2024-09-01"
pages_build_output_dir = "out"
compatibility_flags = ["nodejs_compat"]

# For GitHub-connected Cloudflare Pages, set these in Dashboard once:
# 1) Settings → Environment variables → ADMIN_TOKEN = a strong password/token
# 2) Settings → Functions → KV namespace bindings → AVIDKIYA_KV = your KV namespace
