# AvidKiya v1.3.0

تاریخ انتشار: 2026-07-30

## پر کردن فضای خالی صفحه اصلی

- فضای خالی سمت راست/زیر Hero با باکس‌های داینامیک سایت‌های دیگر پر شد.
- باکس‌های سایت‌ها دقیقاً زیر توضیحات و آمار Hero قرار می‌گیرند تا صفحه پرتر و متعادل‌تر دیده شود.
- بخش پایین قبلی ابزارها/فروشگاه حذف شد و به‌عنوان fallback به بالا منتقل شد.

## باکس‌های داینامیک سایت‌های دیگر

- ساختار جدید `externalSites` به CMS اضافه شد.
- هر باکس شامل موارد زیر است:
  - آیکون
  - اسم سایت
  - توضیحات سایت
  - لینک ورود به سایت
  - رنگ accent
  - وضعیت فعال/غیرفعال
- اگر هیچ سایتی از پنل ساخته نشده باشد، باکس‌های پیش‌فرض ابزارها و فروشگاه به‌صورت خودکار بالا نمایش داده می‌شوند.

## پنل مدیریت

- بخش جدید «سایت‌های دیگر» به Admin Console اضافه شد.
- از پنل می‌توان سایت جدید اضافه کرد، نام/توضیح/لینک/آیکون/رنگ را تغییر داد و سایت را فعال یا حذف کرد.

## نسخه CMS

- نسخه CMS به v4 ارتقا یافت.
- storage key مرورگر به `avidkiya_cms_v4` تغییر کرد تا ساختار جدید بدون تداخل با دیتای قدیمی لود شود.

## تست

Build پروژه با موفقیت انجام شد:

```bash
npm run build
```
