'use client';
import { useCms } from '@/contexts/CmsContext';import { Icon } from '@/components/ui/Icon';
export function Footer(){const{cms,resolve}=useCms();return <footer className="no-print border-t border-border/70 py-8"><div className="mx-auto flex max-w-[1180px] flex-col items-center justify-between gap-4 px-4 text-sm text-muted md:flex-row"><div>© {new Date().getFullYear()} {resolve(cms.identity.fullName)} — {resolve(cms.identity.title)}</div><div className="flex gap-2">{cms.socials.map(s=><a key={s.id} href={s.url} target="_blank" className="grid h-9 w-9 place-items-center rounded-lg border border-border"><Icon name={s.icon} size={16}/></a>)}</div></div></footer>}
