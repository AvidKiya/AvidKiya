'use client';
import { useState } from 'react';import { useCms } from '@/contexts/CmsContext';import { useApp } from '@/contexts/AppContext';import { Icon } from '@/components/ui/Icon';
export function CommentsPage(){const{cms}=useCms();const{lang}=useApp();const[form,setForm]=useState({name:'',email:'',role:'',rating:5,text:''});const[ok,setOk]=useState(false);async function submit(){await fetch('/api/comments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}).catch(()=>{});setOk(true)}const comments=cms.comments.filter(c=>c.approved).sort((a,b)=>(b.pinned?1:0)-(a.pinned?1:0));return <section className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="glass rounded-os p-6"><h1 className="mb-5 text-2xl font-black">{lang==='fa'?'ثبت نظر':'Leave a comment'}</h1><div className="space-y-3"><input className="input" placeholder={lang==='fa'?'نام':'Name'} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" placeholder={lang==='fa'?'سمت':'Role'} value={form.role} onChange={e=>setForm({...form,role:e.target.value})}/><select className="input" value={form.rating} onChange={e=>setForm({...form,rating:Number(e.target.value)})}>{[5,4,3,2,1].map(x=><option key={x} value={x}>{'⭐'.repeat(x)}</option>)}</select><textarea className="input min-h-32" placeholder={lang==='fa'?'متن نظر':'Comment'} value={form.text} onChange={e=>setForm({...form,text:e.target.value})}/><button className="btn btn-primary w-full" onClick={submit}><Icon name="send"/> {lang==='fa'?'ارسال برای تایید':'Submit for moderation'}</button>{ok&&<div className="text-sm text-emerald">✓ {lang==='fa'?'بعد از تایید نمایش داده می‌شود':'Visible after approval'}</div>}</div></div><div><h2 className="section-title mb-5">{lang==='fa'?'نظرات تاییدشده':'Approved comments'}</h2><div className="grid gap-4">{comments.map(c=><article key={c.id} className="glass rounded-2xl p-5"><div className="flex items-start gap-3"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-full bg-primary text-lg font-black text-white">{c.name[0]}</div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h3 className="font-black">{c.name}</h3>{c.role&&<span className="tag">{c.role}</span>}{c.pinned&&<span className="tag text-amber">PINNED</span>}</div><div className="my-2 text-amber">{'★'.repeat(c.rating)}</div><p className="leading-7 text-muted">{c.text}</p></div></div></article>)}</div></div></section>}
