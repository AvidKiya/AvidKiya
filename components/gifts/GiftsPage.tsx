'use client';
import { useCms } from '@/contexts/CmsContext';import { useApp } from '@/contexts/AppContext';import { Icon } from '@/components/ui/Icon';
export function GiftsPage(){const{cms,resolve}=useCms();const{lang}=useApp();return <section><div className="mb-8 text-center"><h1 className="section-title gradient-text">{resolve(cms.gifts.title)}</h1><p className="mt-3 text-muted">{resolve(cms.gifts.subtitle)}</p></div><div className="grid gap-6 lg:grid-cols-2"><div className="glass rounded-os p-6"><h2 className="mb-5 text-2xl font-black">🎁 {resolve(cms.gifts.downloadTitle)}</h2><div className="grid gap-4">{cms.gifts.downloads.map(d=><a key={d.id} href={d.url} className="flex items-center gap-4 rounded-2xl border border-border bg-panel p-4 hover:border-primaryBright"><div className="grid h-12 w-12 place-items-center rounded-xl bg-primary/20"><Icon name="download" className="text-primaryBright"/></div><div className="flex-1"><div className="font-black">{resolve(d.title)}</div><p className="text-sm text-muted">{resolve(d.description)}</p></div><span className="tag">{d.type}</span></a>)}</div></div><div className="glass rounded-os p-6"><h2 className="mb-5 text-2xl font-black">💚 {lang==='fa'?'هدیه شما به من':'Your gift to me'}</h2><div className="grid gap-4">{cms.gifts.donationLinks.map(d=><a key={d.id} href={d.url} className="rounded-2xl border border-border p-5 font-black text-white shadow-glow" style={{background:gradient(d.kind)}}>{resolve(d.label)} →</a>)}</div></div></div></section>}
function gradient(k:string){return k==='bitcoin'?'linear-gradient(135deg,#f59e0b,#78350f)':k==='coffee'?'linear-gradient(135deg,#ff813f,#7c2d12)':'linear-gradient(135deg,#2141a8,#5d7ae6)'}
