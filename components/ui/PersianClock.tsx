'use client';
import { useEffect, useState } from 'react';
import { useApp } from '@/contexts/AppContext';
import { achaemenidEvents, getPersianDate, toFaDigits, todayOrNextEvent } from '@/lib/persian-calendar';
export function PersianClock(){const{lang}=useApp();const[now,setNow]=useState(new Date());useEffect(()=>{const id=setInterval(()=>setNow(new Date()),1000);return()=>clearInterval(id)},[]);const p=getPersianDate(now);const time=now.toLocaleTimeString(lang==='fa'?'fa-IR':'en-GB',{hour12:false});const ev=todayOrNextEvent(lang);return <section className="glass relative overflow-hidden rounded-os p-5"><div className="scan-line opacity-50"/><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><div className="text-sm font-black text-emerald">{lang==='fa'?'تقویم پارسی + هخامنشی':'Persian + Achaemenid calendar'}</div><div className="mt-1 text-2xl font-black">{lang==='fa'?p.formatted:new Intl.DateTimeFormat('en-US-u-ca-persian',{weekday:'long',year:'numeric',month:'long',day:'numeric'}).format(now)}</div><div className="mt-1 text-sm text-muted">{lang==='fa'?'سال هخامنشی: ':'Achaemenid year: '}{lang==='fa'?toFaDigits(p.achaemenidYear):p.achaemenidYear}</div></div><div className="text-center"><div className="mono text-4xl font-black text-primaryBright">{lang==='fa'?toFaDigits(time):time}</div><div className="mt-1 text-sm text-muted">{ev.isToday?(lang==='fa'?'مناسبت امروز':'Today'):(lang==='fa'?'مناسبت پیش‌رو':'Upcoming')}: <b className="text-text">{ev.event}</b></div></div></div><div className="mt-5 flex flex-wrap gap-2">{achaemenidEvents.map(e=><span key={`${e.month}-${e.day}`} className="tag">{lang==='fa'?toFaDigits(`${e.month}/${e.day}`):`${e.month}/${e.day}`} — {e.name[lang]}</span>)}</div></section>}
