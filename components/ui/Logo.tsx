'use client';
import { useCms } from '@/contexts/CmsContext';
export function Logo() { const { cms, resolve } = useCms(); return <div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl border border-border bg-[radial-gradient(circle_at_30%_20%,var(--primary-bright),var(--primary))] text-sm font-black text-white shadow-glow">{cms.brand.logoImage ? <img src={cms.brand.logoImage} alt="logo" className="h-full w-full rounded-xl object-cover"/> : cms.brand.logoLetter}</div><div className="leading-tight"><div className="font-black tracking-tight">{resolve(cms.brand.brandName)}</div><div className="mono text-[10px] uppercase text-muted">devhub.kernel</div></div></div> }
