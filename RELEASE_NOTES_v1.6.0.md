# AvidKiya v1.6.0

تاریخ انتشار: 2026-07-30

## اصلاح ماژول‌ها و کنترل‌های ابزارها

- ظاهر select ساده و بدفرم بخش فشرده‌سازی عکس حذف شد.
- انتخاب فرمت خروجی عکس به کنترل Segmented/Pill هماهنگ با UI سایت تبدیل شد.
- انتخاب الگوریتم هش از select ساده به کنترل Segmented/Pill تبدیل شد.
- اسلایدرهای کیفیت عکس و طول رمز با استایل اختصاصی Liquid/Glass بازطراحی شدند.
- استایل عمومی selectهای باقی‌مانده نیز پولیش شد تا dropdown خام مرورگر کمتر دیده شود.

## نشانگر موس جدید

- نشانگر قبلی حذف و با WebGL Ghost Cursor جایگزین شد.
- پیاده‌سازی بر اساس کد ارسالی انجام شد، بدون پنل lil-gui و مناسب سایت اصلی.
- نشانگر شامل trail نرم، فرم ghost، صورت و واکنش به حرکت موس است.
- روی دستگاه‌های touch غیرفعال می‌ماند.

## تست

Build پروژه با موفقیت انجام شد:

```bash
npm run build
```
