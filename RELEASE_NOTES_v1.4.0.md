# AvidKiya v1.4.0

تاریخ انتشار: 2026-07-30

## پنل مدیریت واقعی‌تر

- بخش «لوگو و برند» به پنل اضافه شد.
- امکان آپلود لوگوی شخصی از پنل اضافه شد تا جای حرف A در هدر قرار بگیرد.
- امکان حذف لوگو و برگشت به حرف جایگزین اضافه شد.
- امکان تغییر نام برند، حرف لوگو، نام فارسی/انگلیسی، عنوان، ایمیل و Bio اضافه شد.
- بخش فروشگاه کامل‌تر شد: اضافه کردن محصول جدید، تغییر قیمت تومانی، فعال/غیرفعال و حذف محصول.
- بخش خدمات کامل‌تر شد: اضافه کردن خدمت جدید، تغییر تعرفه تومانی، فعال/غیرفعال و حذف خدمت.
- بخش سایت‌های دیگر قبلی حفظ شد و برای مدیریت باکس‌های داینامیک صفحه اصلی استفاده می‌شود.

## لوگوی هدر

- هدر حالا اگر `brand.logoImage` وجود داشته باشد، تصویر لوگو را نمایش می‌دهد.
- اگر لوگو آپلود نشده باشد، همان حرف جایگزین مثل A نمایش داده می‌شود.

## دسکتاپ و UI/UX

- عرض اصلی صفحه و هدر از حالت باریک به `1500px` افزایش پیدا کرد تا در دسکتاپ بهتر از عرض صفحه استفاده شود.
- فاصله‌گذاری دسکتاپ بهبود پیدا کرد تا سایت کوچک و جمع‌شده دیده نشود.

## نشانگر موس اختصاصی

- نشانگر موس اختصاصی Liquid/Glow اضافه شد.
- روی لینک‌ها، دکمه‌ها و کارت‌ها حالت hover نشانگر تغییر می‌کند.
- در دستگاه‌های لمسی غیرفعال است.

## پت گربه نارنجی

- یک گربه نارنجی تعاملی به سایت اضافه شد.
- گربه روی صفحه حرکت می‌کند.
- با موس می‌توان آن را گرفت و هر جای صفحه گذاشت.
- موقعیت گربه در localStorage ذخیره می‌شود.
- با دوبار کلیک می‌تواند حالت خواب/بیدار بگیرد.

## تست

Build پروژه با موفقیت انجام شد:

```bash
npm run build
```
